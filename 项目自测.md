检查项	过关要求
对象关系	不看代码画出 TcpServer / Acceptor / EventLoop / Poller / Channel / Connection 的 ownership 与 reference 关系
写过图了，暂且不书

建连路径	从 epoll_wait() / accept 到 Connection 注册完整讲通:

1. 关于连接： Tcpserver有一个acceptor的指针，以及管理的一个端口，端口监听-> acceotor连接-〉返回一个clinet-fd，然后由于有setNewConnectionCallback，直接调用了server的newConnection，然后拓展了connections.



读事件路径	readable event → Channel → Connection → 用户 callback 完整讲通

2. 首先是有poller监听到一个可读性事件，把它放进了eventloop的active_channels，active_channels在eventloop中，用eventloop调用poll()，然后调用active_channels的channel->handleevent, 其中channel的handleEvent在构造的就已经初始化成功了，于是就读数据就行了，毕竟只负责的是基础I/O功能。但是我不知道这是怎么和connection扯上联系的，估计是因为在构造conection的时候，就setReadCallback了，然后把这个channel作为自己的client_channel_，与clinet_fd一起放进了tcpserver的哈希表


写事件路径	为什么需要 output buffer、什么时候监听 EPOLLOUT、partial write 怎么继续
3. 写事件路径与读事件路径跑通的流程差不多，无非就是setWriteCallback,output buffer用来作为缓冲区，一般来说写到EAGAIN的时候就要监听 EPOLLOUT，partial write的情况就是先监听 EPOLLOUT，然后等有信号了在看过来


关闭路径	read()==0、主动 close、错误关闭分别怎么走
4. read()==0 ：ReadResult::PeerClosed
    主动 close：ReadResult::PeerClosed
    错误关闭：  ReadResult::Error;
    因为我们打算关闭这个connection，于是调用了tcpserver注入的removeConnection，把它移出了，然后智能指针自动释放。

生命周期	能指出 callback 捕获 this、Channel 与 Connection 销毁顺序可能产生的风险

5. 我们使用了智能指针来管理，Connection。每个channel在创建的时候都会自动enableReading，然后自动loop_->updateChannel，然后poll->updateChannel,
由Poller负责removeChannel，但是removeChannel与removeConnection我不太明白执行顺序


看这个链条可以很好理解：
为了实现Connection 构造
→ Channel 构造
→ connection给channel来set callbacks
→ enableReading
→ update，channel要准备进入EventLoop，进入Poller
→ EventLoop
→ Poller
→ ADD，通过poller来ADD
→ epoll_wait
→ revents
→ Channel::handleEvent，到时候就自动运行channel早已设计好的回调函数来
→ callback
→ Connection，channel的回调函数会影响connection的状态

enableWriting → MOD
disableWriting → MOD
removeChannel → DEL


epoll 状态	能解释 add/mod/del 分别由谁触发，以及对象状态与 kernel epoll 状态不一致有什么后果
感觉这一块我的代码还没有完成


非阻塞语义	EAGAIN/EWOULDBLOCK 为什么不是错误；ET 下为什么通常要读到 EAGAIN
较基础，暂时掠过

线程模型	明确目前哪些对象只能在哪个线程访问
目前reactor是单线程


Channel 有哪些注册状态？

enableReading 时：
当前状态 → 新状态 → epoll_ctl 什么操作？

enableWriting 时：
当前状态 → 新状态 → 什么操作？

disableWriting 时：
？

remove 时：
？

Connection close 时：
1.
2.
3.
4.


Connection 收到 peer close
↓
我现在正在 callback 栈里面
↓
能不能立刻 erase 自己？
↓
如果不能，谁负责延迟销毁？
↓
Poller 中 Channel 什么时候移除？
↓
fd 什么时候 close？


Connection 收到 PeerClosed 后，谁有权决定它要被删除？
client_channel_ 应该在什么时候执行 ToQuit / DEL？
client_fd_ 应该在什么时候 close()？
如果删除请求发生在 Channel::handleEvent() callback 内部，什么时候才允许真正 connections.erase()？

标准的删除路线：由于connection拥有channel的所有权，所以负责释放cahnnel，server负责erase.connection，然后connection自动析构。所以我们第一步先把channel 引用给摘出 poller。

## 以ReadResult::PeerClosed为例子：
本来把connection的handleread（）函数传到client_channel_里面的handleEvent，但是如果析构connection的逻辑就这么进行的话，channel也要被析构了，然而此时channel::handleevent还在运行，所以要修改

但是要什么时候执行quit_channel呢？
如果信号从connection进入
我认为先做一个检查，如果poller中还有该channel的引用权，那么就把它删除掉.

谁拥有“关闭状态”？connection自己拥有关闭状态，但是受到外界 
谁负责 epoll 注销？
谁拥有最终 delete 权


Connection
   ↓ close callback
TcpServer::requestClose(connection)
   ↓
注销 Channel / 标记待删除
   ↓
当前 handleEvent() 返回
   ↓
稍后真正 removeConnection()
