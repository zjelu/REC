
Linux
 │
 │ EPOLLIN
 ▼
Poller
 │
 ▼
Channel
 │
 │ read callback
 ▼
TcpConnection::handleRead()
 │
 │ recv
 ▼
input buffer
 │
 │ message callback
 ▼
ChatServer::onMessage()
 │
 ▼
parse
 │
 ▼
executeChatCommand()

一开始：
fd=7 readable
这个信息非常底层。
经过 TcpConnection 后：
connection A received message
再经过 ChatServer：
user Alice wants to join room X

抽象层级越来越高。
TcpServer：
负责一个 TCP 服务整体生命周期。

组合 Acceptor；
管理当前 TcpConnection；
新连接建立时创建 TcpConnection；
连接关闭时移除 TcpConnection；
向 TcpConnection 设置上层 callback。

Acceptor
拥有 listen_fd；
负责 socket/bind/listen/accept；
拥有 listen Channel；
把 listen Channel 注册给 Eventloop。

TcpConnection
拥有 client_fd；
拥有该 client 的 Channel；
负责 recv/send；
管理 input/output buffer；
处理连接关闭；
把完整消息通过 callback 交给业务层。

Eventloop
拥有 Poller；
循环调用 poll()；
得到 active Channels；
调用 Channel::handleEvent()。

它不知道聊天室，也不知道 HTTP。

Poller
封装 epoll；
维护 fd/Channel 的对应关系；
负责 epoll_ctl / epoll_wait；
不负责业务逻辑。

Channel
代表“一个 fd 的事件入口”；
记录关心的事件；
记录实际发生的事件；
保存 read/write/close/error callbacks；
不负责聊天业务逻辑。


TcpServer
 │
 ├── owns Acceptor
 │        │
 │        ├── owns listen_fd
 │        └── owns listen Channel
 │
 └── owns/manages 

 TcpConnections
           │
           ├── owns client_fd
           └── owns client Channel

Eventloop
 │
 └── owns Poller
          │
          └── references Channels



例如 listen channel：

listen_channel_.setReadCallback(
    [this] {
        handleRead();
    }
);

这个 callback 指向：

Acceptor::handleRead()


而 client channel：
channel_.setReadCallback(
    [this] {
        handleRead();
    }
);

这个 callback 指向：

TcpConnection::handleRead()


今日（9.6）
今天完成了什么？
1. EventLoop / Poller / Channel 链路 （看笔记）
2. TcpServer 与 Connection 的回调关系（看笔记额）
3. main 与 CMake 链接

今天真正理解了什么？
1. (a, b) 是逗号表达式 （不能随便乱写构造函数）
2. 构造函数不等于默认构造 
3. unique_ptr 可以保持 Connection 地址稳定 （有适合组合不太行就去尝试只能指针，毕竟这有点像是策略模式）

4. include 头文件不等于链接实现（cmake好好理解）

目前还不确定什么？
1. Connection 删除时的生命周期 
2. Acceptor 的 fd 所有权
3. EPOLLOUT 续写是否真正跑通


先给 Reactor 收尾，而不是继续扩展
达到下面标准后就冻结 v0：
- Echo 能通过 nc 跑通；
- 能处理半条、多条消息；
- 部分写与 EPOLLOUT 正确；
- 连接能够安全关闭；
- 有一份事件路径与生命周期说明；
- 至少有一个简单自动化测试。

## 建议的三条学习路线
1. 自己写小项目
继续负责：
- 定义职责；
- 设计接口；
- 建立不变量；
- 完成端到端功能。
当前任务就是收口 Reactor v0.1。

2. 阅读成熟源码，比如Muduo
cceptor::handleRead
→ TcpServer::newConnection
→ TcpConnection::connectEstablished
→ TcpConnection::handleRead/handleWrite
→ TcpServer::removeConnectionInLoop

3. 试着接触开源issue
尝试复现问题，缩小复现，
